import { ProductsTotalPipe } from './products-total.pipe';

describe('ProductsTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
